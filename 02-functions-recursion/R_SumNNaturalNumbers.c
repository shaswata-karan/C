// Print sum of N natural numbers

#include <stdio.h>

void Sum(int n);

int main()
{
    Sum(n);
    return 0;
}

// recursive function
void Sum(int n)
{
    if (count == )
    {
        return;
    }
    printf("Hello World\n");
    printHW(n - 1);
}